#include <stdio.h>

/**
 * main - function entry point
 * Description: this function displays the dtring within the string quotes
 * Return: always (0) as an indicator of success
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\"");
	return (0);
}
