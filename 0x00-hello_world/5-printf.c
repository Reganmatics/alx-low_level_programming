#include <stdio.h>
/**
 * main- function entry point
 * Description: displays the text within string quotes to the console
 * Return: always (0) as an indicator of success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
